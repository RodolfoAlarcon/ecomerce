/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['minimalin-demo.myshopify.com', 'fastworld.negociorodante.com', ],
      },
};

export default nextConfig;
